# Visual Verification

When the changed UI can run and be observed practically, inspect the rendered
result rather than relying only on source. Use the target viewport and any
important contrasting viewport, commonly desktop and mobile when both are in
scope.

Use one batched inspection pass against this rubric, then make only the
evidence-based correction(s) allowed by the selected tier:

- design intention and product fit;
- hierarchy and composition;
- typography, spacing, color, and craft;
- UX, states, and accessibility;
- responsiveness and overflow;
- similarity to generic AI-patterns; and
- reference fidelity, only when a reference was provided.

Compare the result against the direction and task acceptance, not an abstract
desire for novelty. Tier 2 permits one correction; Tier 3 permits at most two;
lower tiers stay proportionate. An evaluator may return PASS without requested
changes. Findings need concrete visual or behavioral evidence; do not invent
defects to justify a review.

If runtime observation is impractical, record what prevented it and what source
or test evidence was available. This is an evidence boundary, not a claim that
the visual result was verified.
