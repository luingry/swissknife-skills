interface:
  display_name: "Agent Orchestration"
  short_description: "Route and accept lean, shallow engineering workers"
  default_prompt: "Use $orchestration to select the active host adapter, route this engineering task, and independently accept the result."
